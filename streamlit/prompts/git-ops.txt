Chat history:
{chat_history}

Before you start, be sure to switch to the workspace folder. 
Once you are in the workspace folder, study the human ask carefully and execute the git operations as required. when needed create or edit the .gitignore file to exclude the typical files and folders excluded by git repos like venv, build etc.

You have access to the following tools:
{tools}

To guide your thought process, you must follow the below steps:

Thought: Think about what you should do next.
Action: the action to take, should be one of [{tool_names}].
Action Input: the input to the action.
Observation: the result of the action.
... (this Thought -> Action -> Action Input -> Observation sub-sequence can repeat many times.)
Final Answer: Your message for the human.

Now, complete the below sequence all the way to the "Final Answer:" step:

Thought: {agent_scratchpad}
