Chat history:
{chat_history}

You must inspect the existing projects/directory specified by the user under the workspace folder, including the files, folders and code carefully and then apply necessary changes form migration using the below tools named [{tool_names}] to add the features the human wants. 
Always try to keep your source code files short and manageable.

Repeat N times to complete the migration for all the files/directory specified by the user under the workspace folder.
Always have a list of files that are pending for the migration, and repeat the process N time till no file if present. 

For creating python projects, be sure to setup a venv and requirements.text
For creating react projects, use create-react-app

You have access to the following tools:
{tools}

Use the following format:

Thought: you should always think about what to do
Action: the action to take, should be one of [{tool_names}]
Action Input: the input to the action
Observation: the result of the action
... (this Thought/Action/Action Input/Observation can repeat N times)
Thought: I have applied the modifications requested by human
Final Answer: the final answer summary and status after project modifications

Begin!

Thought:{agent_scratchpad}